# 详细研究规划：Ti64 β 相区晶粒长大 + Al/V 溶质耦合的三维加速计算

> 2026-09-15 版。目标：18 个月，产出 P1（方法）+ P2（材料）两篇论文。
> 前提：课题组无现成 3D 界面追踪代码；目标体系 Ti-6Al-4V；必须三维。

---

## 第零节 · 起点更正（先看这个）

### 更正 A：Fe–Cr 那篇论文没有公开代码

arXiv:2406.08437（期刊版 *Comput. Mater. Sci.* **247**:113509, 2025，Guin / Linda / Lo / Bhowmick / Mukherjee，IIT Kanpur）经穷尽检索：**无 GitHub / Zenodo / figshare / 数据可用性声明**。

**唯一容易误认的相关代码**：同组的 **μ2mech**（`github.com/mu2mech/mu2mech`，CC BY-NC-SA 4.0）。但它是 **C + FFTW 谱方法解 Cahn–Hilliard，做二元合金的调幅分解与析出相长大**——**不含多序参量晶粒长大、不含 GrainTracker、不含晶界偏析模型**。**不能当作该论文的代码用。**

### 更正 B：MOOSE 的 "Fe–Cr 教程" 不是你要的

路径 `modules/phase_field/tutorials/spinodal_decomposition/`，做的是 **500 °C 下 Fe–Cr 的调幅分解**（Cahn–Hilliard 两相分离），**与晶粒长大无关**。

### 更正 C（最重要）：没有任何开源代码能直接做"3D 多晶 + 多组元溶质 + 偏析"

这是本轮调查最硬的结论：

> **MOOSE 的 GrainTracker（多晶粒追踪，负责晶粒消失/重映射）与巨势多相模型（多组元溶质）之间，没有任何官方示例、测试、教程、文档页、issue 或 PR 证明两者被一起用过。**

准确表述是：**"没有官方设计上的对接，属于未被验证的组合"**，而不是"设计上互斥"（找不到禁止性检查）。

**这意味着：你不是"改一套代码"，而是要"把两套零件装成一台机器"。** 这是本项目最大的工程量，必须在第 0 阶段就验证。

### 更正 D：MOOSE 里有一个"有零件、无样机"的捷径

`PolycrystalKernelAction` / `GrainGrowthAction` 有一个**可选参数 `c`（"Name of coupled concentration variable"）**，设了它，action 会为每个序参量自动加一个 **`ACGBPoly`** 核（晶界—溶质相互作用项）。

**但**：
- **没有任何随包发布的输入文件真的打开过这个 `c`**（`grain_growth/test.i` 未设）
- `ACGBPoly` 只是**晶界处浓度依赖的体驱动力项**，**不是完整的多组元扩散热力学**
- 它属于 "Undocumented Class"

所以这是个**起点，不是成品**。

---

## 第一节 · 平台选型

### 三个候选

| | MOOSE | MICROSIM | PRISMS-PF |
|---|---|---|---|
| 许可 | 开源 | **GPLv3** | LGPL |
| 位置 | `idaholab/moose` | `github.com/ICME-India/MicroSim` | `prisms-center/phaseField` |
| 晶粒追踪 | **GrainTracker，最成熟** | 不明确 | 有 order-parameter remapping（739 晶粒演示，62× 缩减） |
| KKS / 巨势 | 有（但未与 GrainTracker 组合过） | **内置，且是卖点** | 有 CHAC / 析出（KKS+弹性）app |
| CALPHAD | 需自己接 | **内置耦合** | 需自己接 |
| GPU | 有限 | **有（CUDA/OpenCL）** | v3.0 起有 |
| 文档/社区 | **最好** | 小、年轻（2024） | 好 |
| 3D 多晶 | **`grain_growth_3D.i` 现成** | 需验证 | 有 |

### 推荐：MOOSE，但要认清工作是"装配"不是"改造"

**理由**：你的创新点**恰好落在 GrainTracker 工作的地方**——晶粒消失、序参量重映射、拓扑事件。MOOSE 是开源里唯一把"多晶粒追踪 + 拓扑事件处理"做到生产级的。

**备选**：若第 0 阶段验证发现 GrainTracker + 溶质场无法协同，**转 MICROSIM**（KKS + CALPHAD 内置，但你要自己解决多晶粒追踪），或退到 MOOSE 的 Route C（固定晶粒集，不用 GrainTracker）。

### MOOSE 内可复用的现成资产（精确路径）

```
modules/phase_field/examples/grain_growth/
  grain_growth_2D_graintracker.i     ← 入门：2D 铜多晶 100 晶粒 / 8 序参量
  grain_growth_3D.i                  ← 3D 版（100 晶粒 / 18 序参量）
  test_problem.i                     ← GrainTracker 全部配置看得最清
  grain_growth_2D_random.i
  grain_growth_linearized_interface.i

modules/phase_field/test/tests/grain_tracker_test/
  grain_tracker_remapping_test.i     ← 重映射机制的核心测试
  tests                              ← 含 test_recovery_*（checkpoint/recover）、tolerate_remap_failure

modules/phase_field/test/tests/GrandPotentialPFM/
  GrandPotentialMultiphase.i         ← 巨势多相 + 多组元扩散的框架
  SinteringBase.i                    ← 巨势烧结（op_num=4，显式 4 晶粒）

modules/phase_field/examples/multiphase/
  GrandPotential3Phase.i             ← 3 相 3 晶粒，对应 Aagesen PRE 98:023309
  GrandPotential3Phase_masscons.i    ← 严格质量守恒版（仅抛物线自由能可用）

modules/combined/examples/phase_field-mechanics/
  EBSD_reconstruction_grain_growth_mech.i   ← 110 晶粒 / op_num=8 + 弹性
  kks_mechanics_VTS.i                       ← KKS + 弹性
```

### 同行范例（代码不公开，只有论文——但方程可复现）

- **Bandyopadhyay et al., *MSMSE* 31 (2023) 065019** —— CALPHAD（**TTTI3**）驱动的 Ti 合金定量多相场，**在 MOOSE 中求解**：13 个非保守序参量（α 的 12 个变体 + β 基体）+ **KKS** 保守浓度场 + Khachaturyan–Shatalov 弹性能。**这是"MOOSE + Ti 合金多相场 + 溶质"最强的同行范例。**
- **Kelley & Ramulu, *Heliyon* 10(4):e25971 (2024)** —— **MOOSE** 中逐层热 FEM + **1D KKS** 相场描述 Ti64 的 β→α/β 生长，NIMS 库构造 α/β 自由能，预测 β-transus ≈ 1110 K。**与"MOOSE + Ti64 + 溶质扩散"最贴近的已发表工作**（但相场是 1D，不做多晶粒长大）。
- **Greenquist et al., *J. Nucl. Mater.* 532 (2020) 152052** —— 600 颗粒 3D 巨势烧结，**用了 GrainTracker**。但那是**更窄的专实现**（固相晶粒 η + 孔洞 φ + 空位化学势），**不能外推到 GrandPotentialMultiphase**。

**明确的空白**：未找到任何 MOOSE 做 Ti64 晶粒长大（多序参量 + GrainTracker）、或 Ti64 溶质晶界偏析 + 拖曳的公开工作。

---

## 第二节 · MOOSE 内的四条技术路线

| 路线 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A** | `PolycrystalKernelAction` + GrainTracker + 独立 Cahn–Hilliard 浓度场（靠 `c` 参数启用 `ACGBPoly`） | **最保守**，多晶粒追踪成熟 | `ACGBPoly` 只是体驱动力项，**不是完整多组元扩散**；无随包示例 |
| **B** | 两相 KKS + 溶质 + 力学（`kks_mechanics_VTS.i`） | KKS 热力学正确 | KKS 是"每相浓度 + 两相"；**晶粒数 = 序参量数，无 GrainTracker 降维** |
| **C** | 巨势 + **固定晶粒集**（不用 GrainTracker） | **唯一有公开文献支撑的"巨势 + 多晶粒 + 多组元"形态** | **变量数 = 晶粒数**，3D 下很快不可承受 |
| **D** | 巨势 + GrainTracker | 兼顾两者 | **属全新开发，无任何先例**；重映射对化学场守恒性的影响未知 |

### 推荐：**先 A 后 D**

**阶段一（P1 方法开发）走路线 A。** 理由：你的创新点是**守恒转移算子**，它作用在"晶粒消失"这个事件上。路线 A 已经有成熟的 GrainTracker 处理这个事件、有 `c` 场可以承载守恒量——**你正好可以在这里把转移算子做出来并证明它有效**。此时不需要完整 CALPHAD 热力学。

**阶段二（P2 材料结果）再评估是否升级到路线 D**，或把 CALPHAD 热力学接进路线 A 的浓度场。

**必须先做的验证（见阶段 0a）**：确认路线 A 里 `c` 场能否在 GrainTracker 重映射中存活并守恒。

### 路线 D 的三个已知技术风险（若要走）

1. **重映射的对称性前提**：`GrainTracker::swapSolutionValues()` **物理地交换非线性变量的解**。这只有在被交换的序参量**数学上完全可互换**时才正确。纯晶粒长大模型里所有 η 对称，故成立；巨势模型中同相晶粒之间大概率可行，**跨相之间必然不可交换**（这正是 `reserve_op` 存在的原因）。
2. **非晶粒序参量必须排除**：`additional_ops`、孔洞序参量、化学势变量都要靠 `reserve_op` / 变量列表隔离在 tracker 管理之外。
3. **Action 与 Tracker 的衔接未验证**：`GrandPotentialKernelAction` 生成的核/初值是否与 tracker 的 `polycrystal_ic_uo` 交接（`prepopulateState`）兼容，**无文档保证**，需读源码自行确认。

---

## 第三节 · 详细分阶段规划

### 阶段 0a · 平台可行性验证（6–8 周）★ 第一个 go/no-go

**这一步的妙处：验证实验本身就是你要测量的东西。**

**任务清单**
1. 装 MOOSE（建议 `--with-docs` 便于本地查文档），跑通 `grain_growth_2D_graintracker.i`
2. 以它为底，加一个 Cahn–Hilliard 浓度场 `c`（参考 `tutorials/spinodal_decomposition/s1_testmodel.i` 的写法）
3. 在 `PolycrystalKernelAction` 中设 `c = <你的浓度变量名>`，启用 `ACGBPoly`
4. 跑 20–50 晶粒、让晶粒自然长大并消失
5. **仪器化**：输出 `c` 的总积分随时间的曲线，并在图上标出每个晶粒消失事件的时刻

**要回答的问题**
- `c` 场在 `swapSolutionValues()` 之后是否还正确？（重映射会不会把溶质场搞乱）
- 每次晶粒消失，`c` 的总量跳变多少？
- 跳变是否随步长减小而收敛？还是不收敛（说明是结构性问题）？

**交付物**：一张"溶质总量 vs 时间"的曲线，晶粒消失事件标注其上。**这张图就是 P1 论文的第一张图。**

**go / no-go**
- 若 `c` 场在重映射中存活、总量跳变可控制 → **继续路线 A**
- 若被摧毁或跳变不可控 → 转 MICROSIM，或退到路线 C（固定晶粒集，放弃 GrainTracker，改用"人工删除晶粒"的脚本控制拓扑事件——这反而更贴近你的转移算子）

> 这一步无论结果如何都有价值：即使 MOOSE 路线失败，你得到的是"现有最成熟的开源多晶粒追踪器无法保守地处理溶质"这个结论，**本身就是很好的问题陈述**。

---

### 阶段 0b · Ti64 热力学与迁移率接入（6–10 周）

**这是另一个独立的风险点，建议与 0a 并行。**

**核心问题：Ti-Al-V 的热力学与原子迁移率数据从哪来？**

- **商业**：Thermo-Calc 的 **TTTI3** 钛合金库（Bandyopadhyay 等 2023 用的就是它）。**需确认课题组授权。**
- **文献参数**：β 相（BCC）原子迁移率有公开评估——**Huang et al., *J. Phase Equilib. Diffus.* 31(2):135–143, 2010**；Chen 等做过 α 与 β 两相的迁移率评估。这些是**参数表**，可以自己实现。
- **注意**：不同作者基于不同热力学描述得到的迁移率参数**差异很大**（Wang 等用三元 β 相描述，结果与 Huang 等明显不同）。**选哪一套要记录并做敏感性分析。**

**任务清单**
1. 确定数据库来源（Thermo-Calc 或文献参数）
2. 把 Ti-Al-V 的 β 相自由能与原子迁移率写成 MOOSE 的 `DerivativeParsedMaterial`（或接 CALPHAD 接口）
3. **验证**：复现已知量——β-transus 温度、Al/V 在 α/β 间的分配系数、互扩散系数

**go / no-go**：若复现不出文献的分配系数与扩散系数，说明热力学接入有问题，必须先解决再往下走。

---

### 阶段 1 · 2D Ti64 β 参考解（8–12 周）

**目标**：得到一个可信的、含守恒溶质场的 Ti64 β 晶粒长大参考求解器。**没有它就没有训练标签。**

- Ti64 **β 单相区**（>~980 °C），溶质 Al + V，50–100 晶粒，2D
- 含晶界偏析（V 富集）与溶质拖曳
- **对标**：
  - Ti64 β 相区静态粗化的**元胞自动机模型**（含溶质拖曳 + 各向异性迁移率，文献里有，已用于 Ti-6Al-4V / Ti17 / TG6 / TA15）——最直接的竞品
  - Liu & Shin 的 β 凝固多相多组元相场（Al/V 分配系数、交叉扩散）
  - 长大指数、晶粒尺寸分布
- **同时建立数据管线**：输出格式对齐 OPMD benchmark 的约定

**交付物**：一个能跑的 2D 参考求解器 + 与文献对标的验证报告。

---

### 阶段 2 · C1 核心 —— 守恒转移算子（12–16 周）

**这是要发表的核心。**

**任务清单**
1. 把"晶粒消失"显式化为**复形手术**：识别事件 → 删除 3-cell → 施加转移算子 T
2. 实现四个版本的 T 并对比：
   - ① **朴素删除**（不守恒）—— 基线，展示问题
   - ② **体积加权平均**（守恒但物理错——**这正是 SRD 类做法**）
   - ③ **界面通量加权**（按消失晶粒边界上的瞬时扩散通量分配）
   - ④ **学习型 T**（可微、端到端训练）
3. **守恒性验证**：10³ 次以上拓扑事件后，总溶质量守恒到机器精度；记录每次事件的瞬时误差
4. **★ 关键实验**：证明不同的 T 导致**可观测的物理差异**——
   - 最终晶粒尺寸分布
   - 晶界偏析谱（V 的界面过量分布）
   - 有效拖曳力 / 长大指数
   - 并做**误差传播分析**：一次错误分配的影响如何随时间演化（呼应 Wu 1994：守恒约束的形式决定临界半径）

**为什么这个实验是全文的核心**：它把"重分配规则"从数值细节变成**有物理后果的建模决策**。没有这一步，审稿人会说"这只是个数值技巧"。

**交付物**：P1 论文的全部核心结果。

---

### 阶段 3 · C2 耦合一致性（10–12 周）

- 把收敛判据**从更新量改为物理残差**，做对照实验展示**假不动点确实会出现**（这是可以直接发表的负面结果）
- 收缩性保障：谱归一化 / 不动点正则（参考 IRNO, ICML 2026）
- **拓扑感知触发**：用 Betti 数变化 / 界面 Hausdorff 距离作为回退触发，对照 ANCHOR 的残差 EMA

**注意**：这一步依赖阶段 2 的 3D 复形手术。若时间紧，可与阶段 4 合并。

---

### 阶段 4 · C3 三维规模化 + Ti64 结果（16–20 周）

- 上 3D：从 `grain_growth_3D.i` 出发
- 目标 **10⁴ 晶粒**量级 + 严格守恒的 Al/V 溶质场
- **物理场景锁定**：β 相区的晶粒长大 + V 偏析拖曳。**暂不碰 α/β 两相与 β→α 相变**（复杂度陡增）
- 产出材料学结论，例如：β 晶粒长大动力学对 V 偏析非均匀性的敏感性；异常长大（AGG）的触发条件

**必须对比的效率基线**：混合 CA–PF 比纯 2D 相场快约 **5 个数量级**（文献值）。你的加速比必须在这个量级上有说服力。

---

## 第四节 · 必须自己从零写的部分（工作量清单）

1. **Ti64 多组元（Al、V）自由能 / 化学势 / 迁移率的数据接口**（CALPHAD 接入或文献参数实现）
2. **路线 A 中"多晶粒长大 × 溶质场"的完整输入文件**——MOOSE 只有零件（`ACGBPoly` / `c` / KKS / 巨势核），**没有整机示例**
3. **守恒转移算子 T**——这是核心创新，无先例
4. **（若走路线 D）巨势 + GrainTracker 的结合**——无官方示例，需自行验证重映射对化学场守恒性与界面的影响
5. **与 Ti64 实验的标定流程**（EBSD / β 晶粒尺寸 / α 片层厚度）

---

## 第五节 · 验证与评价指标

**不要只用单步 MSE。** 单步精度不能预测长期稳定性（PDE-Refiner ICLR 2024；MicroEvoEval AAAI 2026——PredRNN 在长时程晶粒长大上 SSIM 崩到 0.263）。

1. **守恒性**——总溶质量相对误差漂移曲线，10³ 次拓扑事件后仍需机器精度
2. **物理保真度**——L-EAPSR（晶粒数/平均晶粒尺寸误差）、L-ETAP（总量守恒）；晶粒尺寸分布；晶粒面数–时间曲线；**溶质再分布的非均匀性**（无人占据的指标）
3. **长时程稳定性**——APEBench 的 rollout 指标；**特别报告"消失时刻是否偏早"**（PINN-Phase 栽在这里：中位残差 −53/−85 步）
4. **加速比与扩展性**——到 10⁴ 晶粒的 scaling

---

## 第六节 · 进度与里程碑

| 时间 | 阶段 | 里程碑 | go/no-go |
|---|---|---|---|
| M1–M2 | 0a 平台验证 | 溶质总量 vs 时间曲线（含晶粒消失事件） | **★ 决定平台是否可行** |
| M1–M3 | 0b 热力学接入（并行） | 复现 β-transus、Al/V 分配系数、扩散系数 | 复现不出则先解决 |
| M4–M6 | 1 · 2D 参考解 | 可跑的 2D Ti64 β 参考求解器 + 对标报告 | |
| M7–M10 | 2 · C1 核心 | 四个 T 版本对比 + 物理差异证明 | **★ 决定 P1 能否成立** |
| M11–M13 | 3 · C2 一致性 | 假不动点的实验证据 + 触发判据 | 可与阶段 4 合并 |
| M14–M18 | 4 · 3D 规模化 | 10⁴ 晶粒 Ti64 β 结果 | |

**论文节奏**：P1 投在 M10–M12；P2 投在 M18–M20。

---

## 第七节 · 风险与对策

| 风险 | 级别 | 对策 |
|---|---|---|
| **GrainTracker + 溶质场无法协同** | **高** | 阶段 0a 提前验证；失败则转 MICROSIM 或路线 C |
| **Ti-Al-V 热力学数据拿不到** | **高** | 提前确认 Thermo-Calc 授权；备选文献参数表（Huang 2010 等） |
| **路线 D（巨势 + GrainTracker）走不通** | 中 | 已有先 A 后 D 的退路 |
| **3D 多序参量内存爆炸** | 中 | GrainTracker 的 `reserve_op` 控制序参量数；`grain_growth_3D.i` 是 18 个序参量管 100 晶粒 |
| **严格质量守恒版本的收敛问题** | 中 | issue #30236：缺 Jacobian 导数导致 Newton 收敛变差。**3D 大规模长时演化用标准版配小步长往往更划算** |
| **审稿人质疑加速比** | 中 | 必须对比混合 CA–PF（快 5 个数量级）与 GraGLeS_3D |
| **审稿人拿 Lavari & Gnanaskandan (arXiv:2609.11776) 质疑新颖性** | 中 | **主动引用并划清界限**：他们做了结构 birth/death/coalescence 的守恒传质，但传递算子由物理推导、非学习得到，且不是多晶溶质 |

---

## 第八节 · 你现在需要确认的两件事

1. **Thermo-Calc / TTTI3 的授权情况？**
   - 有 → 阶段 0b 直接调库，省 4–6 周
   - 没有 → 走文献参数表路线（Huang et al. 2010 的 β 相迁移率等），但需要自己做参数实现与敏感性分析

2. **算力条件？**（是否有 GPU 集群、能开多少核）
   - 决定阶段 4 的晶粒数目标（10³ 还是 10⁴）
   - 注意 MOOSE 的 GPU 支持有限；若算力紧张，MICROSIM 的 CUDA/OpenCL 路线值得重新考虑

---

## 附 · 参考实现路径速查

**起步文件**
- 晶粒长大 + GrainTracker（2D/3D）：`modules/phase_field/examples/grain_growth/grain_growth_2D_graintracker.i`、`grain_growth_3D.i`
- 重映射机制：`modules/phase_field/test/tests/grain_tracker_test/grain_tracker_remapping_test.i`
- 浓度场写法：`modules/phase_field/tutorials/spinodal_decomposition/s1_testmodel.i`
- 巨势多相 + 多组元：`modules/phase_field/test/tests/GrandPotentialPFM/GrandPotentialMultiphase.i`
- 严格质量守恒：`modules/phase_field/examples/multiphase/GrandPotential3Phase_masscons.i`
- KKS + 弹性：`modules/combined/examples/phase_field-mechanics/kks_mechanics_VTS.i`
- 110 晶粒 + 弹性：`modules/combined/examples/phase_field-mechanics/EBSD_reconstruction_grain_growth_mech.i`

**关键源码（路线 D 需要读）**
- `modules/phase_field/src/actions/PolycrystalKernelAction.C`（`c` 参数 → `ACGBPoly`）
- `modules/phase_field/src/kernels/ACGBPoly.C`
- `modules/phase_field/src/postprocessors/GrainTracker.C`（`swapSolutionValues`、`reserve_op`）
- `modules/phase_field/src/actions/GrandPotentialKernelAction.C`

**参考论文**
- Guin et al., *Comput. Mater. Sci.* 247:113509, 2025（Fe–Cr 纳米晶 + 偏析，**无公开代码**）
- Bandyopadhyay et al., *MSMSE* 31:065019, 2023（MOOSE + Ti 合金 + KKS + CALPHAD）
- Kelley & Ramulu, *Heliyon* 10(4):e25971, 2024（MOOSE + Ti64 + 1D KKS）
- Greenquist et al., *J. Nucl. Mater.* 532:152052, 2020（巨势 + GrainTracker，烧结）
- Aagesen et al., *Phys. Rev. E* 98:023309, 2018（巨势多相模型的原始文献）
- Huang et al., *J. Phase Equilib. Diffus.* 31(2):135–143, 2010（Ti-Al-V β 相原子迁移率）

---

> **证据强度说明**：本环境无法直连 GitHub / arXiv / MOOSE 文档（网络策略拦截），以上文件路径来自搜索引擎索引的代码片段与多个 GitHub 镜像（permcody/moose、SudiptaBiswas/moose、laagesen/moose 等）。**建议 clone 最新 `idaholab/moose` 后本地核对路径与文件内容**，尤其是 `grain_growth_3D.i` 中 `op_num` 与 `grain_num` 的实际取值、以及 `kks_mechanics_*.i` 的准确文件名。
