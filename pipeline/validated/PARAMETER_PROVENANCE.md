# 参数溯源表：哪些是指派值、哪些已由理论/文献锁定

> 2026-09-20 建立。**用户 2026-09-20 指示：「我拿不到数据，目前需要的数据先从理论
> 推导与文献中获取」。** 本文件就是这件事的账本。
>
> 规则：**每一个"指派值"都必须在这一行里写清它的来源等级**，三档：
>
> | 等级 | 含义 |
> |---|---|
> | **L（文献）** | 有可引用的文献值，给出处 |
> | **T（理论）** | 有解析/半解析推导，给出推导 |
> | **A（指派）** | 两者都没有 —— **必须写明要找什么、怎么测** |
>
> ⚠ 论文里**不得**把 A 档写成材料常数。

---

## 0 本轮解锁的能力（先记下来，否则下次又卡住）

本机（2026-09-20 实测）：

| 通道 | 状态 |
|---|---|
| `WebFetch` | ❌ **对所有域名**都报 "Unable to verify if domain … is safe to fetch" |
| `WebSearch` | ✅ 可用，但只给**摘要片段**，拿不到表格里的数 |
| WSL `curl` → `arxiv.org` | ✅ 200 |
| WSL `curl` → `api.openalex.org` | ✅ 200 |
| WSL `curl` → `api.crossref.org` | ✅ 200 |
| WSL `curl` → `mdpi.com` | ❌ 403；`osti.gov` 超时；`sciencedirect` ✗ |
| WSL `curl` → `link.springer.com/content/pdf/...` | ✅ **200，能下整篇 PDF** |
| WSL `curl` → 机构仓库（如 `scholarworks.*.edu`） | ✅ **200** |

**⇒ 可行的文献管线**：
1. `validated/lit_lookup.py`（OpenAlex）查题录 + **还原完整摘要**
2. 拿 `best_oa_location.pdf_url`，`curl` 下来
3. `pypdf` 抽文本（已装在 conda `ml` 环境；**只是阅读工具，不是项目依赖**）
4. `grep` 找参数表

⚠ 这一轮能出结果，靠的就是这条管线；只靠 `WebSearch` 的摘要是不够的。

---

## 1 缺口 #3 相关的溶质参数（本轮新拿到的文献值）

**来源**：Z. Wang 等，*Phase Field Simulation of Dendritic Solidification of Ti-6Al-4V
During Additive Manufacturing Process*, **JOM 70 (2018) 2190**，
DOI `10.1007/s11837-018-3057-z`（green OA，全文已核对）。

⚠ **该论文自己就用了抗截留项（antitrapping current）来修溶质截留** ——
与本项目缺口 #3 的修复路线独立吻合。

| 参数 | 论文值 | 本项目现值 | 判读 |
|---|---|---|---|
| 液相扩散 `D_l` | **9.5×10⁻⁹ m²/s** | 2.52e-9 | 论文值大 3.8 倍 |
| 固相扩散 `D_s` | **5×10⁻¹³ m²/s** | 4e-13 | 同量级 ✓ |
| 液相线温度 `T_l` | 1928 K | 1923（已用） | ✓ 一致 |
| 固相线温度 `T_s` | 1878 K | — | 可补 |
| **Gibbs–Thomson 系数 Γ** | **1.88×10⁻⁷ K·m** | — | **可反推界面能** |
| 分配系数 `k` | 0.5 | 0.6303 | ⚠ 见 §1.1 |
| 液相线斜率 `m_l` | −0.088 K/% | 600 K/摩尔分数 | ⚠ 见 §1.2 |

### 1.1 界面能 `γ_SL` —— **T + L 档**

由 `Γ` 反推（定义式 `Γ = γ_SL/ΔS_f,v`，`ΔS_f,v = ΔH_f,v/T_m`）：

```
ΔS_f,v = 1.28e9 / 1941 = 6.59e5 J/(m³·K)
γ_SL = Γ · ΔS_f,v = 1.88e-7 × 6.59e5 = 0.124 J/m²
```

⇒ **`γ_SL = 0.124 J/m²`**（文献 `Γ` 反推）。本项目形核里用的是 **0.2**（区间 0.15~0.3）。
**两者差 1.6 倍，而 `γ_SL³` 进的是形核指数的前置因子 ⇒ 形核率差 ~4 倍。**
⇒ 建议改用 **0.124**，或至少把它标成"0.124（文献 Γ 反推）~0.2（常用值）"的区间。

### 1.2 液相线斜率 `m_L` —— **T 档（van't Hoff）**

论文给的 `−0.088 K/%` **与 Ti64 相图不符**：纯 Ti `T_m = 1941 K`，
Ti64 液相线 ≈ 1928 K，`c_V = 4 wt%` ⇒ 平均 `dT/dc ≈ −3 K/wt%`，
比 `−0.088` 大 30 倍以上。**⇒ 那个值不能用**（论文用的是简化的 `c0 = 10%`、`k = 0.5`，
不像真实 Ti64 热力学）。

用 **van't Hoff（稀溶液、理想溶液）** 自己推：

```
溶剂（Ti）在两相化学势相等 ⇒ R·T·c_l·(1−k) = ΔH_f·(T_m − T)/T_m
而 T_m − T = −m_L·c_l
⇒  m_L = −R·T_m²·(1−k) / ΔH_f
```

代入 Ti：`T_m = 1941 K`、`ΔH_f = 14.15 kJ/mol`、`k = 0.6303`：

```
m_L = −8.314 × 1941² × (1−0.6303) / 14150 = −819 K / 摩尔分数
```

换算：`1 wt% V = 0.94 at%` ⇒ **`m_L ≈ −7.7 K/wt%`**。

**⇒ 本项目现在用的 `600 K/摩尔分数` 与理论值 −819 只差 27%** —— 说明当初那个指派值
方向是对的。**建议改成 −819（T 档）**，并把 600 记为"与之相符的旧指派值"。
⚠ `k` 仍需独立确定（`m_L` 对 `k` 敏感：`k = 0.5` ⇒ `m_L = −1108`；`k = 0.63` ⇒ `−819`）。

### 1.3 `k_V` 的文献值 —— **仍缺（A 档）**

JOM 2018 给 `k = 0.5`，但那是配合它自己的简化成分用的。
**本项目用 `k = 0.6303` 是自洽反解出来的（由 `A_part` 定，T4 验证过偏差 0.0495%）**，
**不是**从 Ti64 相图取的。

**要找的**：Ti-V 二元系在 β-Ti 端的平衡分配系数 `k_V`，以及 Ti64 三元下的值。
**怎么找**：Thermo-Calc 的 TCTI 数据库直接算；或 CALPHAD 评估论文
（Ti-Al-V 三元热力学评估）。
**怎么测**：EPMA/APT 测定向凝固试样的固/液两侧成分。

---

## 2 缺口 #2（形核 → 等轴晶）：**`f(θ)` 与 `A` 可以整个换掉**

### 2.1 现在的做法与它的问题

现在用的是 CNT + 一个**指派**的润湿因子 `f(θ) = 0.00267`（θ = 20°）
和一个**指派**的前置因子 `A = 1e6 /s`。**两个都没有 Ti64 依据。**

### 2.2 文献给的替代路线 —— **Interdependence 模型 + Greer free-growth**

**来源**：*Grain Refinement of Alloys in Fusion-Based Additive Manufacturing Processes*,
**Metall. Mater. Trans. A 51 (2020) 4360**，DOI `10.1007/s11661-020-05880-4`
（hybrid OA，全文已核对）。

该综述**独立确认了本项目已实现的 CET 机制**：
> 「CET occurs when the amount of constitutional supercooling (ΔT_CS)
> exceeds the critical undercooling for nucleation (ΔT_n)」

而它把所需的两个量都给了**可算的形式**：

**(i) 生长限制因子 `Q`（纯理论，用本项目已有的量算）**

```
Q = m_L · C₀ · (k − 1)
```

代入 `m_L = −819`、`C₀ = 0.036`、`k = 0.6303`：

```
Q = (−819) × 0.036 × (0.6303 − 1) = +10.90 K
```

**⇒ 这个量不需要任何新数据。** 它取代了原来"指派"的形核强度，且是标准量
（综述原文：「the most effective solute elements have a high Q value」）。

**(ii) 临界形核过冷 `ΔT_n`（Greer free-growth，纯理论）**

```
ΔT_n = 4·γ_SL / (ΔS_f,v · d_p)
```

`d_p` 是**形核颗粒的直径**。代入 `γ_SL = 0.124`、`ΔS_f,v = 6.59e5`：

| `d_p` | `ΔT_n` |
|---|---|
| 0.1 µm | 7.5 K |
| 0.5 µm | 1.5 K |
| 1 µm | 0.75 K |
| 5 µm | 0.15 K |

**⇒ 把"指派一个接触角"换成了"给一个颗粒直径"** —— 而颗粒直径是**可测的**
（SEM/TEM 测夹杂物尺寸分布），且 LPBF 粉末里的氧化物夹杂尺寸有大量文献。
**这比接触角好得多**：接触角在 Ti 合金 + 氧化物上几乎查不到，颗粒尺寸到处都是。

**(iii) 形核剂**：综述给出的**唯一**一个与"效率"有关的定量事实：
> 「the number of grains nucleated on the added nucleant particles increased
> from **less than 0.1 pct** of the particles activating nucleation to **27 pct**
> in solidification conditions similar to AM」

**⇒ 形核效率 27%（AM 条件）**，这是 `A` 那个前置因子的物理对应物，
可以据此把当前指派的 `A = 1e6 /s` 换成"按颗粒数密度 × 27%"反标。

### 2.3 结论：缺口 #2 的数据缺口**从两个降到半个**

| 原来缺 | 现在 |
|---|---|
| `f(θ)`（润湿角） | ✅ **不需要了** —— 换成 free-growth 的 `ΔT_n = 4γ/(ΔS_v·d_p)` |
| `m_L` | ✅ **理论推出 −819 K/摩尔分数**（§1.2） |
| `A`（前置因子） | 🟡 半解决 —— 物理含义明确为「颗粒数密度 × 27% 效率」，但**数密度仍要查/测** |
| **`d_p`（颗粒直径分布）** | 🔴 **仍需** —— SEM/TEM 或 LPBF Ti64 夹杂物文献 |

---

## 3 缺口 #5（织构 `hwhm`）与缺口 #1（`A_ani`）—— **仍缺，但要什么已明确**

`A_ani` 是**模型侧的标定参数**，标定它需要**一个实测的织构**作为目标。
所以这两个缺口收敛到同一份数据：

| 要找的 | 具体形式 | 怎么找 |
|---|---|---|
| prior-β 的 **⟨001⟩ 纤维织构强度** | MUD（multiples of uniform density）或 pole figure 强度比 | EBSD / 中子衍射；搜 "Ti-6Al-4V LPBF prior beta texture MUD" |
| 该纤维织构的**角宽度** | (001) 极图的 HWHM（度） | 同上 |
| prior-β 柱状晶的**长宽比** | 宽 × 长（µm） | 同上 |

⚠ 本轮实测：**开放 API 对这三项的检索没有给出可直接引用的数值**
（命中 9 篇，摘要是定性的）。⇒ **这三项是本轮没拿下的部分。**

**候选文献**（本轮检索到但**未读到正文**，因为 `sciencedirect` 不通）：
* Liu & Shin, *J. Cryst. Growth* **564** (2021) 126112 — 与本项目 2b 同题，
  很可能**直接给了可反标定的各向异性系数**
* *The influence of microstructural texture and prior beta grain recrystallisation
  on the deformation behaviour of LPBF Ti–6Al–4V*, MSEA (2021), DOI
  `10.1016/j.msea.2021.141546`
* Van Cauwenbergh 等，*Tailoring heat treatments for metals processed by LPBF*（KU Leuven，OA）
* *Solidification and Crystallographic Texture Modeling of LPBF Ti-6Al-4V Using
  Finite Difference-Monte Carlo*, OSTI `2498470`（⚠ osti.gov 本机不通）

---

## 4 缺口 #4（`Q` 无取向差依赖）—— **A 档，但要什么很具体**

晶界扩散激活能 `Q` 随取向差变化：物理上说得通（低 Σ CSL 晶界扩散慢），
但**本项目查不到任何给出"Ti64 β 相 `Q(Δθ)` 曲线"的文献**。

**要找的**：`Q_GB(Δθ)` 或 `D_GB(Δθ)` 对 β-Ti / Ti64 的实验或 MD 数据。
**替代方案**：用 **Read–Shockley 同构**（`Q(Δθ) = Q_0·(Δθ/θ_m)(1 − ln(Δθ/θ_m))`），
把它标成"借用 Read–Shockley 形式，参数未标定"。⚠ 这是**形式类比不是测量**。

---

## 5 缺口 #6（单组元溶质占位）—— **L 档数据大量存在**

现在是单组元 `c`（V）、`c₀ = 0.036`、`k` 由 `A_part` 反解。真实 Ti64 是
**Al + V 二元溶质**（6 wt% Al、4 wt% V），且 Al 是 α 稳定元素、V 是 β 稳定元素，
**两者的 `k` 一个 >1 一个 <1** ⇒ 单组元不代表真实偏析。

**要找的**：Ti-Al-V 三元 CALPHAD 热力学（Thermo-Calc TCTI 系）。
**这不缺文献，缺的是算力/授权** —— 是"换个工具算"，不是"找不到"。
⇒ **建议把它单列为「换工具任务」，不要与"缺数据"混在一起。**

---

## 6 T13（溶质拖曳）—— **判据上测不到，且与文献锚点冲突**

拖曳力比势垒项小 **11 个数量级**。要测到就得放大 `Ω₀`，
而 `Ω₀ = −5e-11` 是 **Cahn 1962 理论 + Tan 2016 的 V 锚点（2.2~5.3 at/nm²）** 定出来的。
**⇒ 放大 `Ω₀` 会推翻那条锚点。** 这是一个**二选一**，不是缺数据。

---

## 7 汇总

| 参数 | 原等级 | 现等级 | 依据 |
|---|---|---|---|
| `m_L` | A（600） | **T**（−819 K/摩尔分数） | van't Hoff |
| `γ_SL` | A（0.2） | **L**（0.124 J/m²） | JOM 2018 的 `Γ = 1.88e-7 K·m` 反推 |
| `f(θ)` | A（0.00267） | ✅ **不再需要** | 换成 free-growth |
| `Q`（生长限制因子） | 无 | **T**（10.90 K） | `m_L·C₀·(k−1)` |
| 形核效率 | A（`A = 1e6 /s`） | **L**（27%） | MMAT 2020 综述 |
| `D_l` | L（但值不同） | 待定 | JOM 2018 给 9.5e-9（本项目 2.52e-9）|
| `D_s` | — | **L**（5e-13） | JOM 2018 |
| `d_p`（颗粒直径） | — | 🔴 **仍缺** | SEM/TEM 或夹杂物文献 |
| 织构 MUD / HWHM | — | 🔴 **仍缺** | EBSD |
| `k_V`（真实三元） | A（反解） | 🔴 **仍缺** | CALPHAD |
| `Q_GB(Δθ)` | — | 🔴 **仍缺** | 无文献；可借 Read–Shockley 形式 |
| Ti-Al-V 热力学 | — | 🟡 **换工具** | Thermo-Calc |

**本轮把 3 个指派值换成了理论/文献值，删掉 1 个（`f(θ)`），
还剩 4 项真缺 + 1 项换工具。**
