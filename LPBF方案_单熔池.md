# LPBF Ti64 单熔池：两阶段晶粒演化加速方案

> 2026-09-17 版。目标：从温度场出发，经「凝固」与「冷却」两个阶段，得到室温晶粒组织。
> 每一阶段配一个神经算子。**第一阶段先只做一个熔池。**
>
> 已定方向（用户 2026-09-17 确认）：
> - 算子职责：**只学溶质**，晶粒几何仍来自相场参考解（保持现架构）
> - 阶段二物理：**只做 β 晶粒长大/粗化**，暂不碰 β→α′ 马氏体
> - 温度场：**自己造**（激光照射 Ti64），不要求绝对精确
> - 规模：**先做一个熔池**，打通再说

---

## 〇、先说风险

**最大风险不在算子，在阶段一的参考求解器。**

GrainTracker 是为「晶粒消失」设计的：它靠 `reserve_op` 回收消失晶粒腾出的序参量槽位。
而 LPBF 凝固是**连续形核**——晶粒不断「诞生」，槽位只出不进。

> **全库没有任何一个示例把 GrainTracker 与液相放在一起（已穷尽检索，零命中）。**

所以阶段一必须先做一个 go/no-go：

> **能不能在连续形核下管住序参量槽位？**
> - 能 → 继续
> - 不能 → 退到方案 B（见 §4）

---

## 一、总体结构

```
   温度场 T(x,y,t)（激光扫描 + 冷却）
        │
        ├──────────────► 阶段一：熔池凝固
        │                熔融 → 凝固，输出「热的」晶粒 + 溶质场
        │                → 算子 A 的数据
        │
        └──────────────► 阶段二：降温
                         热的晶粒 → 室温晶粒（β 长大/粗化）
                         → 算子 B 的数据
```

两个阶段共用同一个温度场模块，但取样窗口不同：
- 阶段一：T 穿过液相线/固相线的窗口
- 阶段二：T 从固相线降到室温的窗口

---

## 二、温度场模块（两阶段共用，先做）

### 物理模型

移动高斯热源的 **Rosenthal 解**（半无限体，准稳态）：

$$T(\mathbf{x},t) - T_0 = \frac{\eta P}{2\pi k R}\exp\!\left(-\frac{v(R + \xi)}{2\alpha}\right)$$

其中 $\xi = x - vt$ 是随激光移动的坐标，$R = \sqrt{\xi^2 + y^2 + z^2}$，
$\eta$ 吸收率、$P$ 激光功率、$v$ 扫描速度、$k$ 导热系数、$\alpha$ 热扩散率。

**为什么用它**：解析、零成本、能给出正确的熔池长宽比与 trailing tail；
参数（P、v、光斑）直接对应工艺参数，便于后面扫参数空间。

**已知局限**（明确记录，不装作没有）：不含对流/Marangoni、不含潜热、不含多次重熔。
对"先打通"足够；要定量对标时换真实 FEM 热场，**接口不用改**（见下）。

### Ti64 参数（待核）

| 量 | 符号 | 取值 | 来源 |
|---|---|---|---|
| 液相线 | $T_L$ | 1928 K | 待核 |
| 固相线 | $T_S$ | 1878 K | 待核 |
| β 转变温度 | $T_\beta$ | 1268 K | 待核 |
| 导热系数 | $k$ | ~20 W/(m·K) | 待核 |
| 热扩散率 | $\alpha$ | ~6e-6 m²/s | 由 k/(ρc) 算 |
| 吸收率 | $\eta$ | ~0.35 | 待核 |

### 已标定结果（2026-09-17，`pipeline/thermal_field.py`）

取 P=300 W、η=0.35、v=0.6 m/s、光斑半径 30 μm、预热 353 K：

| 量 | 值 |
|---|---|
| 熔池宽度 | 95.1 μm |
| 熔池深度 | 47.6 μm |
| 前缘 / 尾部 | 28.8 μm / 513.2 μm |
| 长宽比 | 5.7 |
| 冷却速率 | **1.79×10⁶ K/s** |

冷速落在 LPBF 真实区间（10⁴–10⁷ K/s），且远高于 β→α′ 马氏体形成的临界冷速——
**这与"阶段二暂不做马氏体"的选择是一致的**，但要知道这是简化。

### ⚠️ 发现一个模型内在矛盾（必须知道）

点源 Rosenthal 解在激光**后方**指数因子恒为 1，温度按 $1/|\xi|$ 衰减，于是

$$|\xi_{\text{tail}}| = \frac{\eta P}{2\pi k (T_L - T_0)}$$

**尾部长度与扫描速度无关**——这是点源模型的固有假象（真实光束是分布源，尾部短得多）。

由此产生一个**无法同时满足的两难**：

| 想要 | 做法 | 代价 |
|---|---|---|
| 熔池远大于光束（点源解才有效） | 提高 P | 尾部变长（∝P）→ 计算域必须很大 |
| 尾部短、计算域小 | 降低 P | 熔池缩到小于光束半径 → **点源假设失效** |

当前参数下熔池半宽/光束半径 = 1.59，勉强有效。

**三条出路**（按推荐顺序）：
1. **换分布源解**（移动高斯源的解析/半解析解，或 Goldak 双椭球）——根治，但要写新函数
2. **缩小光束半径**到 15–20 μm（部分高精度 LPBF 机型如此）——让"熔池远大于光束"更容易满足
3. **接受长尾**，把计算域做到 ~1 mm 长——最省事但最贵

### 接口设计（关键：与来源解耦）

温度场做成 **AuxVariable `T`**，由 `FunctionAux` 或 `ParsedAux` 驱动。
只要换 `Functions` 块就能从 Rosenthal 换成真实 FEM 热场，**算子与后处理完全不用改**。

---

## 三、阶段二：降温下的 β 晶粒长大（低风险，先打通）

**这一步几乎零新物理**——`GBEvolutionBase` 的迁移率本来就是 Arrhenius 的，`T` 本来就是耦合变量。

### 改法（相对现有 `phase2_prod.i` 的最小改动）

1. 加 `[AuxVariables] T`，用 `FunctionAux` 驱动一条冷却曲线
2. `GrainGrowthAction` 加 `variable_mobility = true`、`coupled_variables = 'T'`
3. `GBEvolution` 的 `T` 从常数 `450` 改成变量 `T`
4. 换成 Ti64 的真实参数（`wGB`、`GBmob0`、`Q`、`GBenergy`）
5. 顺手修 `A_scale`（见 §5）

### 冷却曲线

$$T(t) = T_{\text{room}} + (T_{\text{solidus}} - T_{\text{room}})\exp(-t/\tau)$$

$\tau$ 由目标冷速定。LPBF 冷速 $10^4$–$10^6$ K/s → $\tau$ 约 $10^{-3}$–$10^{-4}$ s。
**注意**：实际 LPBF 的冷却不是单调指数（后续层会重熔/回火），
但单熔池第一版用单调曲线是合理的，多道时再改。

### 为什么先做这一步

1. **风险最低**：官方有 `temperature_gradient.i` 背书
2. **打通整条链**：T 场 → 相场 → extract.py → 训算子 → 回放，全部跑一遍
3. **它独立于阶段一的设计悬案**：即使阶段一的凝固方案还没定，这一步也能推进

---

## 四、阶段一：熔池凝固（高风险，需先做可行性测试）

### ⚠️ 重要澄清（2026-09-17，用户纠正）

**温度场是"已熔融液态金属"的温度场，我们从这里开始，不从冷固体被激光熔化一遍。**

这带来一个关键简化：**初始条件就是「熔池内已是液态 + 熔池外围是固态多晶」**，
而不是"固态基体被激光加热熔化"。所以不需要建模激光吸收、固→液熔化动力学。

`mu(T)` 机制仍然保留，但角色变了：它不再是"把固体熔掉"，
而是**保证液相线以上液态稳定（不自行结晶）、降到液相线以下才开始凝固**。

### 已跑通的实现（`pipeline/stage1_meltpool.i`）

**核心机制**——不编译 C++，全部用现成对象：

标准晶粒长大的 Allen-Cahn 方程（`GrainGrowthAction` 生成的核）：

$$\frac{\partial\eta}{\partial t} = -L\,\mu\,(\eta^3-\eta+2\gamma\eta\textstyle\sum\eta_j^2) - L\,\kappa\nabla^2\eta$$

`ACGrGrPoly` 的体驱动力**正比于材料属性 `mu`**（属性名硬编码，见 `ACGrGrBase`）。
所以让 `mu` 在液相线以上**变负**，有序态就不再是极小值 → η 被压到 0（液态），
且液态成为**稳定**态（不会自发结晶）。降到液相线以下 `mu` 转正 → 凝固开始。
`kappa_op` 始终为正，界面项不受影响。

$$\mu(T) = \mu_0\left[1-(1+b)\cdot\tfrac12\left(1+\tanh\frac{T-T_{mid}}{\Delta T}\right)\right]$$

用 `DerivativeParsedMaterial` 提供 `mu`（**不能用 `GBEvolution`**，否则属性重复声明冲突）。

**初始熔池**：用 `ParsedSubdomainMeshGenerator` 以 t=0 的 Rosenthal 等温线
（`T > T_liquidus`）精确划出熔池，作为独立 block；Voroni IC 只加在固态 block 上。

### 冒烟测试结果（2026-09-17）

| 检查项 | 结果 |
|---|---|
| 无发散跑完 46 步 | ✅ |
| 熔池保持液态（`liquid_frac` 稳定在 0.172） | ✅ |
| GrainTracker 正常追踪（17–19 个晶粒） | ✅ |
| 序参量槽位未耗尽 | ✅（但时长还不够长） |

**判读注意**：`liquid_frac` 稳定是**正常的**——熔池随激光一起移动（准稳态），
池子面积不变。**凝固要看尾部 η 的再生，不是看 liquid_frac 下降。**

### 四个踩过的坑（`--check-input` 查不出，运行时才炸）

1. `pi` 在解析型网格生成器里**未定义**（`ParsedFunction` 里可用）→ 常数折叠成数值
2. `symbol_values` 只能是数字或对象名，**不能是表达式**；且按空白切分 → 表达式内联
3. 最大步长是 `[Executioner] dtmax`，不是 TimeStepper 的 `max_dt`。
   不加限制时自适应步长一路涨，**超过界面弛豫时间 $1/(L\mu)\approx4.7\,\mu$s 后线性求解发散**
4. `ACGrGrPoly` 的 `mu` 是硬编码属性名，`ACInterface` 的 `mob_name`/`kappa_name` 可配

### 下一步（阶段一）

- 跑完整算例（end_time=6.5e-4，约 330 步）→ 看尾部是否长出**柱状**晶
- 加溶质场 `c`（当前算例没有）
- 检验长时程下序参量槽位是否会耗尽（这是真正的 go/no-go）

---

## 四·原 · 候选方案（保留备查）

### 问题定义

熔池内是液相，熔池边界外是固相。激光移走后，**尾部边界开始外延生长**，
晶粒从部分熔化的基体/前一层**外延**长入熔池。热流方向（≈ 最大温度梯度方向）
决定竞争结果——取向接近该方向的晶粒胜出 → **柱状晶**。

### 候选方案

| 方案 | 做法 | 优点 | 风险 |
|---|---|---|---|
| **A** | `ACSwitching` + 液相序参量（借鉴 `GrandPotentialSolidification.i`，扩到 N 晶粒） | 物理最正统，MOOSE 有现成零件 | 与 GrainTracker 的组合完全未验证；序参量槽位管理是硬骨头 |
| **B** | **两段解耦**：凝固单独跑（可用任何模型，甚至 CA），把结果作为阶段二的初值 | 绕开 GrainTracker 与液相共存的难题；阶段二已能跑 | 需要另一条"能把晶粒结构导出成 `unique_grains`"的路径 |
| **C** | 「熔池擦除」：保持 Ση=1，在熔池内把 η 驱到无序态（全 =1/N），温度相关的阱深让无序态在 T>T_L 稳定 | 改动最小，完全复用现有 `PolycrystalKernelAction` | 无序态是否真是稳定液相需验证；物理近似较粗 |

**推荐：先做 A 的最小可行性测试；不通过则退到 B。**
C 作为 A 的简化变体保留。

### go/no-go 判据

最小算例：2D、一个熔池、预算 ≤30 晶粒、跑到凝固结束。判据：

1. 熔池能形成（T>T_L 区域 η 被正确重置）
2. 尾部能外延生长出**柱状**晶粒（不是等轴随机）
3. **序参量槽位不耗尽**（这是核心）
4. 溶质守恒（复用 `phase0a` 的检查脚本）

---

## 五、必须顺手修的既有问题

### `ACGBPoly` 与自由能差因子 2

`ACGBPoly::computeDFDOP` 返回的耦合项是 $2\varepsilon\mu\gamma c^2\eta_i$，
而 `f_loc` 用 `A_scale = 0.5` 给出 $\varepsilon\mu\gamma c^2\eta_i$。**差 2 倍，变分不自洽。**

**修法**：`phase2_prod.i` 的 `constant_expressions` 里 `A_scale` 从 `0.5` 改成 `1.0`。
（输入文件里"必须一致"的注释是对的，只是数值没对上。）

### 其他

- **`replay_transfer.py` 的「面出现」分支是空实现**（`take = 0.0`）。
  凝固阶段以「诞生」为主，**这一半必须先补上**。
- 只编译了 `phase_field-opt`；若要 KKS/力学示例需 `combined-opt`。

---

## 六、执行顺序

| # | 任务 | 风险 | 状态 | 产出 |
|---|---|---|---|---|
| 1 | 温度场模块（Rosenthal + 冷却曲线），标定熔池尺寸 | 低 | ✅ **完成** | `thermal_field.py`、`lpbf_thermal.i`、`thermal_field.png` |
| 1b | 决定点源 vs 分布源（见上方两难） | 中 | ⬜ 待定 | 影响计算域大小 |
| 2 | 修 `A_scale`；参数换 Ti64 | 低 | ⬜ | 物理自洽的参考解 |
| 3 | **阶段二打通**：T 驱动迁移率 → 数据 → 训算子 → 回放 | 低 | ⬜ | 第二条链全通 |
| 4 | **阶段一可行性测试**（go/no-go） | **高** | ⬜ | 决定 A 还是 B |
| 5 | 补 `replay_transfer.py` 的「面出现」分支 | 中 | ⬜ | 转移算子完整 |
| 6 | 端到端：阶段一 → 阶段二 → 两个算子 | 中 | ⬜ | 单熔池闭环 |

---

## 七、待定/待核

- Ti64 的 $T_L$、$T_S$、$T_\beta$、$k$、$\alpha$、吸收率 —— 需查文献定值
- 阶段一用 2D 还是直接上 3D？（熔池本质是 3D，但 2D 纵截面能体现柱状晶）
- 溶质取 Al、V 双组元还是先用单组元替代？
- 算子容量：转到 LPBF 后输入维度会长到 30–50 维，届时再做宽度/深度扫描定容
